[project]
name = "sanskritai"
version = "0.2.0"
description = "Sanskrit knowledge platform starter project"
requires-python = ">=3.10"
dependencies = [
    "pandas",
    "numpy",
]

[project.optional-dependencies]
dev = ["pytest"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
